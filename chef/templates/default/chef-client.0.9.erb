# Configuration file for the chef-client service

<% [['CONFIG', :config], 
    ['PIDFILE', :pidfile], 
    ['LOCKFILE', :lockfile], 
    ['LOGFILE', :logfile], 
    ['INTERVAL', :interval],
    ['SPLAY', :splay]].each do |k| %>
<%   if @parameters[k[1]] %>
<%=     "#{k[0]}=#{@parameters[k[1]]}" %>

<%   end %>
<% end %>

<% options = "" %>
<% if @parameters[:node] %>
<%    options << "-N #{@parameters[:node]}" %>
<% end %>
<% if ! options.empty? %>
<%  "OPTIONS=#{options}" %>
<% end %>
