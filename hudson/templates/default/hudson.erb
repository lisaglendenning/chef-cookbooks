#!/bin/sh
#
# hudson    Daemon script for Hudson server.
#
# chkconfig: 345 91 10
# description: Hudson

### BEGIN INIT INFO
# Provides: hudson
# Required-Start: $local_fs $network
# Required-Stop: $local_fs $network
# Should-Start: $named $time
# Should-Stop: $named $time
# Short-Description: Startup script for hudson 
# Description: See http://hudson-ci.org/
### END INIT INFO

. /etc/rc.d/init.d/functions

JAVA_HOME="<%= @hudson[:java] %>"
HUDSON_HOME="<%= @hudson[:home] %>"
HUDSON_USER="<%= @hudson[:user] %>"

prog="hudson-server"
exec="${HUDSON_HOME}/${prog}"
pidfile="/var/run/hudson.pid"

[ -f /etc/sysconfig/${prog} ] && . /etc/sysconfig/${prog}

start() {
    [ -x ${exec} ] || exit 5

    action $"Starting ${prog}: "
    daemon --check ${prog} --user ${HUDSON_USER} "HUDSON_HOME=${HUDSON_HOME} JAVA_HOME=${JAVA_HOME} ${exec}" &
    retval=$?
    return $retval
}

stop() {
    action $"Stopping ${prog}: "
    killproc -p ${pidfile} ${prog}
    retval=$?
    return $retval
}


case "$1" in
    start)
        $1
        ;;
    stop)
        $1
        ;;
    status)
        status hudson
        ;;
    restart)
        stop
        start
        ;;
    force-reload)
        stop
        start
        ;;
    *)
        echo $"Usage: $0 {start|stop|status|restart|force-reload}"
        exit 2
        ;;
esac
exit $?
